
#Spring profiles can be "default" or "dev" or "prod" or some custom values 
#Spring profiles let you group confiurations and easily switch between massive groups of configuration easily, by simply chaning the profile. 
spring.profiles.active=prod
spring.application.name=rando

spring.cloud.config.uri=http://localhost:6000

server.port = 9090